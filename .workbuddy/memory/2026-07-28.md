# 2026-07-28 工作日志

- PM 方法论讨论（用户目标：多 AI agent 协同开发，人工仅轻干预如置工单 In Progress 即触发开发）。
  - 结论：产品讨论=最高杠杆 PM 工作，交付「agent 可直接执行的工单 backlog」；开发/验证/QA/修复/PR/状态更新=每工单自治闭环；人工仅在点位（触发、Review&Merge 把关、升级裁决）轻干预。
  - 对齐 AGENTS.md North Star 闭环；提醒：落地首个产出应是低风险工单垂直切片+能力审计，而非再堆 PM 文档。
  - 待用户确认下一步交付物：工单规格模板 / 能力审计 / 人工干预 RACI / 路线图。
  - 用户选定「工单规格模板」为首交付物。已创建 `specs/agent-ready-ticket-template.md`：含 9 个必填字段（标题/目标/范围边界/验收标准/验证方式/依赖/Done 定义/风险回滚/人工点位）+ Linear/Plane 字段映射 + 一个低风险垂直切片填写示例。下一步建议：用此模板把「复用优先能力审计」拆成 1 个真实低风险工单切片跑通闭环。

- 文件整理 + 命名落地（接续 PM 讨论前的工作）：
  - 命名三件套敲定：**Product = Ticket Autopilot**；**Engine = 编排内核**（`src/ticket_autopilot/engine/`，可 import 为 `ticket_autopilot.engine`）；**Connector = 接外部系统的胶水层**（`src/ticket_autopilot/connectors/`，由 `adapters/` 改名）。用户否决 "Handler"（与引擎内部 `handlers/` 步骤处理器撞名 + 颗粒度偏细）。
  - 合并执行：把独立引擎文件夹 `ticket-autopilot/`（前 vivarium-forge-flow / vff）整体收编进 `src/ticket_autopilot/` 作为 `engine/` 子包；`reference/`、`workflows/`、`runs/`、`sandbox/` 一并移入；旧顶层 `ticket-autopilot/` 目录已删除。
  - 清空残留：代码+文档里的 `vff`/`vivarium`/`forge_flow`/`forge-flow`/`Forge Flow` 全部改为 `ticket_autopilot.engine` / `Engine` / `Connector`；`drivers.script_call` 的 handlers 路径由 `"forge_flow"` 修正为 `"engine"`；测试 `PKG_ROOT` 因多嵌一层改为三级 dirname；`adapters/`→`connectors/`。
  - 验证：7 个引擎单元测试全过（mock 跑通 retry loop + CLI 护栏 + Hermes 分发）；`close_ticket` 经相对路径正确定位 `reference/ticket-pipeline/plane_client.py`；`.gitignore` 补回 `sandbox/*`（保留 .gitkeep）。
  - 未提交 git（按规则需显式确认）。注意 runtime 目录 `.ticket-autopilot/`（规格书定义的运行期 worktree/runs 点文件夹）与源码包 `ticket_autopilot` 是两回事，规格书里的 `.ticket-autopilot/...` 引用保持不变。
