# 2026-08-01

## AIO-26 独立验收（QA，只读）— VERDICT: PASS

工单：AIO-26「Recover orphaned PREPARING prompt records so Prepare never deadlocks」
Plane issue `d74403da-40e2-425c-8962-d1266b961be3`｜base `8429bd4`｜head = 未提交 working tree

**改动范围（ticket-owned，归属 CLEAN）**：仅 `src/ticket_autopilot/services/prompt_resolver.py`（+39/−2）与 `tests/test_prompt_resolver.py`（+79）。无 web.py / state.json / UI / 依赖变更。

**实现要点**：`has_active_run()` 新增 `_preparing_is_orphaned()`——`owner_pid` 存活（`os.kill(pid,0)`）则保持单飞返回 True；`ProcessLookupError` 判死亡；`PermissionError` / 兜底 `OSError` 判存活（失效安全）。无 `owner_pid` 的 legacy 记录按 mtime 超过 `_STALE_PREPARING_SECONDS = 30*60` 判孤儿。恢复是读改写，只动 `status/planner_outcome/hard_break_reason` 三键。`prepare()` 新增落盘 `owner_pid` + 带时区 `created_at`。

**验收结论**：五条 AC 全通过；自建独立验证脚本 50/50 全绿（含真实子进程存活/死亡、pid 1 的 PermissionError 真实路径、字段集合差比对、畸形 owner_pid、幂等重扫、损坏 JSON）。

**关键复用经验（下次验收可直接抄）**：
- 证明测试失败是 pre-existing 的干净手法：`git archive HEAD | tar -x -C /tmp/xxx`，在无改动副本上复跑 —— 不动 `git stash`/worktree，完全只读。本次据此判定 `tests/test_web_service.py` 的 2 个失败（`status()` 返回 1；`stop()` 后端口 Errno 48）与本票无关，故 `pytest tests/ -q` 非 exit 0 不计入本票。
- `git diff` 被分页器截断时用 `git --no-pager diff | cat`。
- **`Path.glob` 返回的是文件系统枚举顺序，既非字典序也非创建序**（实测 APFS：创建 zzz/mmm/aaa → 枚举 mmm/zzz/aaa）。任何「同目录多记录 + 单次扫描断言多个结果」的测试都隐性依赖它。

**遗留 findings（均不卡本票，建议各自立票）**：
- F-1 major/pre-existing：AIO-17 web 服务生命周期，`stop()` 非同步、`status()` 在启动窗口误报。
- F-2 minor：新增 legacy 边界测试依赖 glob 顺序，建议拆成两个独立 resolver 的测试或在 `has_active_run()` 里 sort。
- F-3 minor：**`state.json` 分支仍无存活性检查且先于 metadata 循环**，崩溃残留 `state.json=ACTIVE` 仍会让 `prepare()` 永久死锁 —— 票名 "never deadlocks" 实际只覆盖 prompt 记录。范围边界已明确排除，需后续票补齐。
- F-4 minor：`_write_json` 非原子（`write_text`），而 `has_active_run()` 从纯读变成会写；截断 JSON 被 `JSONDecodeError → continue` 吞掉属失效开放。建议复用 `web.py` 的 `atomic_json_write`。

**产物**（刻意写在仓库外，避免污染刚认证为 CLEAN 的 diff 归属）：
`/tmp/aio26_qa/AIO-026-qa-verdict.json`、`/tmp/aio26_qa/AIO-026-qa-report.md`、`/tmp/aio26_qa/verify_ac.py`。
若要入库，按项目约定放 `tasks/AIO-026-qa-verdict.json`；PASS 结论的 qa-verdict 可直接进 `tasks/archive/`。
