# 2026-07-22

## 研究：AI-Operations 项目 + Start Prompt 比较方法

- 项目定位：建设「海粟通用 AI Agent 操作系统提示词（Start Prompt）」。真源 = `core/`（8 段）+ `modules/`（6 任务模块）；发布构建 v4（单文件 ~215 行）与 v5（由 `build_v5.py` 从顶层源重建 ~297 行）；`platform/` 是按工具分发的 delta。
- 现有对比实验 `eval/Benchmark-02`：冻结式、可复现、多模型（4 个）、双盲判分、3 arm（bare/v4/v5）、Wilson 区间裁决、holdout 防过拟合、judge 校准 + 人工审计。已很扎实。
- 关键可复现性事实：`eval/prompts/v5_core` 与 `v5_modules` 是顶层 `core/`、`modules/` 的逐字节副本；`build_v5.py` 实际从**顶层源**读，改顶层源后必须重跑 `build_v5.py`。
- 交付物：`01 Working/Start_Prompt_比较方法研究.md` —— 通用方法 SPCM（9 层）：把「比什么维度/用例/判分」与「具体哪几个 prompt」解耦，并提出 8 条在现有 `eval/` 上的落地改造（泛化 arms、多 judge、定性轨道、agentic 验证、扩用例库、dev-loop profile、跨模型聚合、成本进判据）。
- 现有 eval 相对「对比任意 start prompt」的 9 个缺口已记录在报告中（被 v4/v5 写死、单 judge、开放文本质量 out of scope、纯文本无工具执行、用例窄、维度耦合代码、缺迭代回路、跨模型聚合未固化、成本-质量 Pareto 缺失）。

## 运维：项目启用 Git 版本控制

- 在 `/Users/hzuo/Documents/code/AI-Operations` 初始化 git 仓库（分支 `main`），首个 commit `99fe246`「初始化：AI-Operations 项目结构与文档」，共纳入 61 个文件。
- 新增 `.gitignore`：屏蔽 .DS_Store、Python（__pycache__/venv 等）、Node（node_modules/dist）、*.log、*.bak、编辑器目录。
- `.workbuddy/` 已纳入版本控制（仅含项目记忆日志，无密钥）。未配置远程仓库（remote）—— 用户仅要求本地版本控制。
- 注：提交遵循用户规则需显式确认；后续 commit/push 等写操作仍需先获得用户同意。

## 整理：目录分类归位

- 设计了按"内容类型/功能"分类的方案，并对散落文件归位。
- 关键约束：`eval/build_v5.py` 硬编码读取顶层 `core/` 与 `modules/`（`CORE_DIR = ROOT.parent / "core"`），故 `core/ modules/ platform/ eval/` 四个功能目录**保留在根目录，不可移动**，否则破坏 v5 构建。
- 新分类：`specs/`（Ticket Autopilot v0.1 规格书，从根目录移入）、`research/inbox/`（原 `00 Inbox/` 三份研究笔记）、`research/working/`（原 `01 Working/` 比较方法研究）、`tooling/`（原 `codex-notification-setup/` 挪入）。源空目录 `00 Inbox`/`01 Working` 已删除。
- 发现重复：`research/inbox/Haisu_Universal_Start_Prompt_v4.md` 与 `eval/prompts/v4.md` **字节完全相同（215 行）**。已保留副本（尊重原归类意图），待用户决定是否删冗余。
- 调整：用户认为 research 内分 inbox/working 没必要，已摊平——4 个研究笔记全部直接放 `research/`，删除 `research/inbox`、`research/working` 子目录。约定"以后文件多了再分子文件夹"。

## 整理：core/modules/platform/eval 归入 tooling/start-prompt/

- 用户起初误以为这 4 个文件夹"组成一个评估工具"。已纠正事实：`eval/` 才是评估工具（Benchmark-02）；`core/modules/platform` 是**被评估的 Start Prompt 产品真源**（8 核心段 + 6 任务模块 + 4 工具 delta）。两者是"考题 vs 考卷"。
- 经核查 `eval/build_v5.py`：`CORE_DIR = ROOT.parent/"core"`、`MODULE_DIR = ROOT.parent/"modules"`，即用相对兄弟路径读取。因此将 4 个文件夹**整体**移入 `tooling/start-prompt/`，构建路径仍解析正确（已用 python 验证 CORE_DIR/MODULE_DIR 指向存在目录）。
- 已同步修正 `tooling/start-prompt/eval/README.md`：① `cd` 改为 `.../tooling/start-prompt`；② 加注 eval 现位于 `tooling/start-prompt/eval`、命令需在该父目录运行、4 文件夹须整体移动；③ 将 build_v5.py 描述由"top-level core/modules"改为"sibling core/modules"。
- 当前顶层结构：`AGENTS.md`、`IDEA.md`、`research/`、`specs/`、`tooling/`（含 `start-prompt/`、`codex-notification-setup/`）。
- 注：本次仅移动文件，未提交 git。研究笔记 `research/Start_Prompt_比较方法研究.md` 内对 `core/ modules/ eval/` 的相对描述未同步更新（为说明性文字、不可直接运行，低优先级）。
- AGENTS.md / IDEA.md 保留根目录（项目元信息，约定俗成）。
