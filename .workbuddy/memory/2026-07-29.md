# 2026-07-29

## 多 agent 协同团队架构 + 未启动票排序（PM 建议）
- 用户规划 agent 团队：dev ×2 互为备份 = Claude Code + 「pi」(pi 身份未确认，待问)；QA ×2 互为备份 = QoderWake CLI + Codex；Plan = WorkBuddy CLI（用户问"有没有 cli 模式"）。
- 实测：本机无 `workbuddy`/`wb` CLI 二进制（PATH、`/usr/local/bin`、`/Applications/WorkBuddy.app` 均为 Electron，无 CLI）。→ Plan agent fallback 待定（Claude Code plan-mode 或手动 PM）。
- Board 现状：completed=#2/#3；started=#1/#4/#5/#6；unstarted=#7/#8/#9。用户说"剩下3个没完成" → 解读为 3 个 unstarted（#7/#8/#9）；#1/#4/#5/#6 已 started 不计入。
- **推荐次序（3 个未启动票）**：#9 安全护栏 → #8 连接器 → #7 Codex QA。理由：先立 CLI 沙箱再放 dev agents 自动执行（安全优先）；连接器接通自动关单/建PR（闭环可跑）；最后接独立 QA 闸门（依赖前两者）。依赖已顺。
- **指出缺口**：#7 工单当前只写 Codex，要落实"2个QA互为备份"需把 QoderWake CLI 也接进 Verify（扩 #7 或加接线）。dev agents = Execute 劳力，实现 #9/#8/#7 及未来票；connector(#8) = 自动 Close。
- 待用户确认：①"3个"是否=#7/#8/#9；②pi 身份；③WorkBuddy CLI 是否存在（fallback 选择）；④是否扩 #7 接 QoderWake CLI。
- **已确认（用户回复）**：①"3个"=#7/#8/#9；③WorkBuddy CLI 本机无 → Plan 改用 **Codex CLI**。②pi 仍未确认；④待定（建议扩 #7 接 QoderWake CLI 以落实 2QA 备份）。
- **落实动作建议（待用户批准才写 Plane）**：(a) 把 #9 安全护栏优先级/排期提到 #7/#8 之前；(b) 修正 #7/#8 依赖编号错位（#7 依赖应为 #8 而非 #6；#8 依赖应为 #9 而非 #5）；(c) #6–#9 加进 Cycle 1 + 补 label；(d) 扩 #7 范围接 QoderWake CLI。这些属多写，需用户确认。

## 第二次归档开发/验收提示词（07-29）
- 用户再次要求"将已经完成的开发提示词和测试提示词移动到存档文件夹"。
- 拉 board：completed 现为 #2/#3/#7/#8（#7/#8 的 completed_at = 07-29 凌晨，对比昨天新增）。已把 `AIO-007-*`、`AIO-008-*` 共 4 个提示词 mv 进 `tasks/archive/`。
- **发现不一致**：archive 里已有 `AIO-001`/`AIO-004`/`AIO-006`，但它们的 Plane 票（#1/#4/#6）状态仍是 started，非 completed。这违反"仅 completed 才归档"的原规则，疑似上一轮之后被用户/执行 agent 按"提示词写完"口径提前归档。active 区现仅剩 #5(started)、#9(unstarted)。
- 待与用户确认：#1/#4/#6 是留在 archive 还是退回 active；归档规则到底按"Plane completed"还是"提示词写完"。未提交 git。
- **用户裁决（07-29 02:43）**：① #1/#4/#6 退回归档前的 active（严格按"提示词仅 completed 票才归档"，started 的 #1/#4/#6 不顺带归档）；② QA 验证产物**结论 pass/accept 即可进 archive**，独立于票状态。已执行：mv #1/#4/#6 提示词回 active；mv `AIO-005-qa-verdict.json`(accept) + `AIO-008-qa-verdict.json`(PASS) 进 archive。终版归档规则已写入 MEMORY.md。未提交 git。

## 9 张票完成后的能力盘点 + Web UI 决策（07-29 用户提问）
- **Plane 状态与代码不一致**：git 显示 AIO-4/7/8/9 均已 commit（含 `AIO-9 enforce Engine CLI guardrails`、PR #1 aio-8 plane-pr-connector）。但 API 拉 board：Done=#2/#3/#7/#8；In Review=#1/#4/#5/#6；Todo=#9。即**代码都做完了，但 #9 和 4 张 In Review 的 Plane 状态未同步**——典型"自述完成≠证据完成"，建议补 sync。
- **已验证能力映射**（读源码 + mock 自测确认）：
  - AIO-2 → Engine 核心 `engine.py`（YAML→DAG+verify-reject-retry 闭环+快照）
  - AIO-6 → `drivers.py`（llm/cli/hermes/script/mock 五类执行器）
  - AIO-9 → `guardrails.py`（cwd 沙箱+只读强制+工具白名单，spawn 前拦截）
  - AIO-7 → `connectors/qa.py` + `schemas/qa-verdict.schema.json`（只读 QA→结构化 verdict→确定性校验→"无伪成功"门禁）
  - AIO-8 → `connectors/plane.py`(取票/建 workflow/关单置 Done) + `connectors/github.py`(建草稿 PR，永不自动 merge，head 必须独立分支)
  - AIO-1/3/4/5 → 审计结论 / 脚手架 / 九字段工单模板 / 垂直切片证据(runs/*.json)
- **Mock 自测跑通**：`PYTHONPATH=src python3 -m ticket_autopilot.engine run src/ticket_autopilot/workflows/ticket-pipeline.yaml --mock --params '{"ticket_id":"DEMO-1"}'` → plan→execute→verify(拒1)→execute→verify(接受)→close，快照落 `runs/`。
- **两点漂移需注意**：① 产品级 CLI `ticket-controller`(`src/ticket_autopilot/cli.py`)仍是 stub（TODO 未接），真入口是 Engine CLI；② README 写"Linear Ticket/Claude 开发"，实际实现是 Plane + qodercli(QA)。
- **Web UI 决策（PM 建议：暂缓自建）**：v0.1 显式把 trigger+Web UI 列为 out-of-scope（engine README 明说），且 AGENTS.md 红线=复用优先/不预造平台。非技术用户的真阻塞是凭证(env)、本地 Python/git、读 YAML/verdict，非"命令行"本身。更优路径：先收口 AIO-5 垂直切片 → 接真 ticket-controller CLI → 做**只读状态看板**(FastAPI+极简 HTML，读 runs/*.json) 直击"交付不直观"痛点；或 UI 层直接由 QoderWake 提供（复用优先）。真要做也先从只读仪表盘起，不做控制台。
